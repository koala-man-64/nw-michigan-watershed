name: Build and Deploy React App

on:
  push:
    branches: [ main ]  # Adjust if needed

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      # Other steps like building your React app, Docker image, etc.

      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # Steps to login to ACR, push Docker image, and restart your container instance would follow...
