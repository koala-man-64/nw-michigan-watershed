# Production Static Data Validation Runbook

This file is retained as a legacy redirect.

The portal no longer validates by fetching tracked CSV files directly from Static Web Apps, and the old `scripts/validate_static_data.py` workflow is not part of the current repository.

Use [prod-runtime-validation.md](./prod-runtime-validation.md) instead. That runbook matches the current API-backed portal and managed API deployment model.
